services:
  memento-mori:
    build: .
    volumes:
      - ./:/app/workspace
      - ./output:/app/output
    environment:
      - PYTHONUNBUFFERED=1
    command: --search-dir /app/workspace --output /app/output